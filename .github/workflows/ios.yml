name: Build & Upload iOS (Flutter) — VERBOSE (Fixed Packaging + Entitlements Proof)

on:
  workflow_dispatch:
    inputs:
      export_method:
        description: "Export method. Use 'ad-hoc' for 3uTools/device install, 'app-store-connect' for App Store upload."
        required: true
        default: "ad-hoc"
        type: choice
        options:
          - ad-hoc
          - app-store-connect
  push:
    branches: [ versionios4 ]

jobs:
  ios:
    runs-on: macos-15

    env:
      BUNDLE_ID: com.primejet.gas2door
      TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      P12_BASE64: ${{ secrets.P12_BASE64 }}
      P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
      MOBILEPROVISION_BASE64: ${{ secrets.MOBILEPROVISION_BASE64 }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 16
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16.1"

      - name: Pin DEVELOPER_DIR (safety)
        run: echo "DEVELOPER_DIR=/Applications/Xcode_16.1.app" >> $GITHUB_ENV

      - name: Print runner & toolchain versions
        run: |
          set -euxo pipefail
          sw_vers || true
          xcodebuild -version
          xcrun --version
          swift --version
          ruby -v
          python3 --version || true
          node -v || true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter doctor
        run: |
          set -euxo pipefail
          flutter --version
          flutter doctor -v

      - name: Flutter clean & deps (order matters)
        run: |
          set -euxo pipefail
          flutter clean
          flutter pub get
          test -f ios/Flutter/Generated.xcconfig || { echo "ERROR: ios/Flutter/Generated.xcconfig missing"; ls -la ios/Flutter || true; exit 1; }

      - name: Bump iOS build number (Timestamp Strategy)
        run: |
          set -euxo pipefail
          PLIST="ios/Runner/Info.plist"
          NEW_BUILD_NUM=$(date +%Y%m%d%H%M)

          /usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" "$PLIST" >/dev/null 2>&1 || \
            /usr/libexec/PlistBuddy -c "Add :CFBundleShortVersionString string 1.0.0" "$PLIST"

          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $NEW_BUILD_NUM" "$PLIST" || \
            /usr/libexec/PlistBuddy -c "Add :CFBundleVersion string $NEW_BUILD_NUM" "$PLIST"

          echo "Build number updated to: $NEW_BUILD_NUM"

      - name: Handle Encryption Compliance (Auto-answer 'No')
        run: |
          set -euxo pipefail
          PLIST="ios/Runner/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PLIST" || \
            /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PLIST"

      - name: Set marketing version to match App Store (1.0.2)
        run: |
          set -euxo pipefail
          PLIST="ios/Runner/Info.plist"
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 1.0.2" "$PLIST" || \
            /usr/libexec/PlistBuddy -c "Add :CFBundleShortVersionString string 1.0.2" "$PLIST"
          echo "Marketing version forced to 1.0.2"

      - name: Ensure CocoaPods toolchain
        run: |
          set -euxo pipefail
          sudo gem install cocoapods --no-document
          pod --version
          pod repo update

      - name: Force iOS 15 in Podfile (idempotent)
        run: |
          set -euxo pipefail
          ruby -e "f='ios/Podfile'; if File.exist?(f); c=File.read(f); c=c.gsub(/platform :ios, .*/, \"platform :ios, '15.0'\"); File.write(f,c); else; puts 'WARN: ios/Podfile not found'; end"

      - name: Import signing certificate (keychain)
        env:
          P12_BASE64: ${{ secrets.P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
        run: |
          set -euxo pipefail
          echo "$P12_BASE64" | base64 --decode > signing.p12
          security create-keychain -p "" build.keychain
          security import signing.p12 -P "$P12_PASSWORD" -A -k build.keychain
          security list-keychains -s build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security set-keychain-settings -lut 21600 build.keychain
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "" build.keychain
          security find-identity -v -p codesigning build.keychain || true

      - name: Decode & parse provisioning profile (install under UUID)
        shell: bash
        env:
          MOBILEPROVISION_BASE64: ${{ secrets.MOBILEPROVISION_BASE64 }}
        run: |
          set -euxo pipefail
          PP_DIR="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PP_DIR"

          TMP_PP="/tmp/profile.mobileprovision"
          if printf '%s' "$MOBILEPROVISION_BASE64" | base64 -D > "$TMP_PP" 2>/dev/null; then :
          elif printf '%s' "$MOBILEPROVISION_BASE64" | base64 --decode > "$TMP_PP" 2>/dev/null; then :
          elif printf '%s' "$MOBILEPROVISION_BASE64" | base64 -d > "$TMP_PP" 2>/dev/null; then :
          else
            echo "Failed to base64-decode MOBILEPROVISION_BASE64" >&2; exit 1
          fi

          security cms -D -i "$TMP_PP" -o /tmp/profile.plist
          /usr/bin/plutil -lint /tmp/profile.plist
          NAME=$(/usr/libexec/PlistBuddy -c 'Print :Name' /tmp/profile.plist)
          UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' /tmp/profile.plist)
          APPID=$(/usr/libexec/PlistBuddy -c 'Print :Entitlements:application-identifier' /tmp/profile.plist || true)
          : "${NAME:?Provisioning profile Name missing}"
          : "${UUID:?Provisioning profile UUID missing}"

          INSTALL_PP="$PP_DIR/${UUID}.mobileprovision"
          cp "$TMP_PP" "$INSTALL_PP"

          echo "PROFILE_NAME=$NAME" >> $GITHUB_ENV
          echo "PROFILE_UUID=$UUID" >> $GITHUB_ENV
          echo "Installed profile: $NAME ($UUID) at $INSTALL_PP"
          echo "App Identifier in profile: ${APPID:-unknown}"

          mkdir -p build-logs
          cp /tmp/profile.plist build-logs/profile.plist

      - name: Inject signing + entitlements into Release.xcconfig (critical)
        shell: bash
        env:
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          PROFILE_NAME: ${{ env.PROFILE_NAME }}
        run: |
          set -euxo pipefail
          : "${PROFILE_NAME:?PROFILE_NAME not found. Ensure the decode/parse step ran successfully.}"

          XCCONFIG="ios/Flutter/Release.xcconfig"
          grep -q 'Pods-Runner.release.xcconfig' "$XCCONFIG" || echo '#include "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"' >> "$XCCONFIG"
          sed -i '' '/^\/\/ --- Signing injected by CI ---/,$d' "$XCCONFIG" || true

          {
            echo '// --- Signing injected by CI ---'
            echo "PRODUCT_BUNDLE_IDENTIFIER = ${BUNDLE_ID}"
            echo 'CODE_SIGN_STYLE = Manual'
            echo 'CODE_SIGN_IDENTITY = Apple Distribution'
            echo "PROVISIONING_PROFILE_SPECIFIER = ${PROFILE_NAME}"
            echo "DEVELOPMENT_TEAM = ${TEAM_ID}"
            # THIS is the key line that proves/forces your Sign in with Apple entitlement to be used in CI signing:
            echo 'CODE_SIGN_ENTITLEMENTS = Runner/Runner.entitlements'
          } >> "$XCCONFIG"

          echo "==== Final Release.xcconfig ===="
          cat "$XCCONFIG"

          mkdir -p build-logs
          cp "$XCCONFIG" build-logs/Release.xcconfig

      - name: Reset CocoaPods resolution (clean resolve)
        working-directory: ios
        run: |
          set -euxo pipefail
          rm -rf Pods Podfile.lock
          pod repo update

      - name: Pod install (log & artifact)
        working-directory: ios
        run: |
          set -euxo pipefail
          pod install | tee ../build-logs/pod_install.log

      - name: Write ExportOptions.plist (method is selectable)
        working-directory: ios
        env:
          PROFILE_NAME: ${{ env.PROFILE_NAME }}
          BUNDLE_ID: ${{ env.BUNDLE_ID }}
          TEAM_ID: ${{ env.TEAM_ID }}
          EXPORT_METHOD: ${{ inputs.export_method }}
        run: |
          set -euxo pipefail

          # If this is a push trigger (not workflow_dispatch), default to ad-hoc for sanity unless overridden
          if [ -z "${EXPORT_METHOD:-}" ]; then
            EXPORT_METHOD="ad-hoc"
          fi

          cat > ExportOptions.plist <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>compileBitcode</key><false/>
            <key>destination</key><string>export</string>
            <key>method</key><string>${EXPORT_METHOD}</string>
            <key>signingStyle</key><string>manual</string>
            <key>teamID</key><string>${TEAM_ID}</string>
            <key>uploadSymbols</key><true/>
            <key>provisioningProfiles</key>
            <dict>
              <key>${BUNDLE_ID}</key>
              <string>${PROFILE_NAME}</string>
            </dict>
            <key>signingCertificate</key><string>Apple Distribution</string>
          </dict>
          </plist>
          PLIST

          echo "==== ExportOptions.plist ===="
          cat ExportOptions.plist

      - name: Fix Xcode Project Structure (only if fix_project.rb exists)
        run: |
          set -euxo pipefail
          if [ -f "fix_project.rb" ]; then
            gem install xcodeproj --no-document
            ruby fix_project.rb
          else
            echo "No fix_project.rb found. Skipping."
          fi

      - name: Build IPA (Release, verbose)
        run: |
          set -euxo pipefail

          # IMPORTANT: Build via Flutter so flutter_assets is packaged correctly
          flutter build ipa --release --export-options-plist=ios/ExportOptions.plist --verbose

          mkdir -p build-logs
          RESULT=$(ls -t ~/Library/Developer/Xcode/DerivedData/*/Logs/Build/*.xcresult 2>/dev/null | head -n 1 || true)
          if [ -n "${RESULT:-}" ]; then
            echo "Found xcresult: $RESULT"
            /usr/bin/xcresulttool get --format json --path "$RESULT" > build-logs/archive_result.json || true
          fi

          ls -la build/ios/ipa || true
          ls -la build/ios/archive || true

      - name: Validate IPA contents (assets + entitlements) — stops the cycle
        run: |
          set -euxo pipefail

          IPA_PATH="$(find build/ios/ipa -maxdepth 1 -type f -name '*.ipa' -print -quit 2>/dev/null || true)"
          echo "Resolved IPA_PATH=${IPA_PATH:-<none>}"
          test -n "${IPA_PATH:-}" || { echo "❌ No IPA produced in build/ios/ipa"; exit 1; }

          rm -rf ipa_check
          mkdir -p ipa_check
          cp "$IPA_PATH" ipa_check/app.ipa
          cd ipa_check
          unzip -q app.ipa

          APP_PATH="$(find Payload -maxdepth 2 -name '*.app' -print -quit)"
          echo "Resolved APP_PATH=${APP_PATH:-<none>}"
          test -n "${APP_PATH:-}" || { echo "❌ No .app found in IPA Payload"; exit 1; }

          echo "== Confirm Flutter assets exist =="
          ls -la "$APP_PATH/flutter_assets" || true
          test -f "$APP_PATH/flutter_assets/AssetManifest.json" || { echo "❌ Missing flutter_assets/AssetManifest.json (broken IPA packaging)"; exit 1; }
          echo "✅ AssetManifest.json exists."

          echo "== Print bundle id =="
          /usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "$APP_PATH/Info.plist" || true

          echo "== Print entitlements embedded in signed app (the truth) =="
          codesign -d --entitlements :- "$APP_PATH" || true

          echo "== Decode embedded profile and print its entitlements =="
          test -f "$APP_PATH/embedded.mobileprovision" || { echo "❌ embedded.mobileprovision missing in app"; exit 1; }
          security cms -D -i "$APP_PATH/embedded.mobileprovision" > /tmp/embedded_profile.plist
          /usr/libexec/PlistBuddy -c "Print :Entitlements" /tmp/embedded_profile.plist || true

      - name: Diagnose effective build settings (Release)
        working-directory: ios
        run: |
          set -euxo pipefail
          xcodebuild -workspace Runner.xcworkspace -scheme Runner -configuration Release -showBuildSettings | tee ../build-logs/xcodebuild_effective_settings.txt
          grep -E 'PRODUCT_BUNDLE_IDENTIFIER|CODE_SIGN_STYLE|CODE_SIGN_IDENTITY|DEVELOPMENT_TEAM|PROVISIONING_PROFILE_SPECIFIER|CODE_SIGN_ENTITLEMENTS' ../build-logs/xcodebuild_effective_settings.txt || true

      - name: Save IPA artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: |
            build/ios/ipa/*.ipa
            ipa_check/app.ipa
          if-no-files-found: warn

      - name: Upload troubleshooting bundle (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-troubleshooting-bundle
          path: |
            build-logs/**
            ios/ExportOptions.plist
            ios/Runner.xcodeproj/project.pbxproj
            ios/Runner.xcworkspace/contents.xcworkspacedata
            ios/Podfile
            ios/Podfile.lock
            ios/Pods/Manifest.lock
          if-no-files-found: warn
