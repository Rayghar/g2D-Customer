name: Build & Upload iOS (Flutter) — VERBOSE

on:
  workflow_dispatch:
  push:
    branches: [ versionios2 ]

jobs:
  ios:
    runs-on: macos-14

    env:
      BUNDLE_ID: com.primejet.gas2door
      TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      P12_BASE64: ${{ secrets.P12_BASE64 }}
      P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
      MOBILEPROVISION_BASE64: ${{ secrets.MOBILEPROVISION_BASE64 }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print runner & toolchain versions
        run: |
          set -euxo pipefail
          sw_vers
          system_profiler SPSoftwareDataType || true
          xcodebuild -version
          xcrun --version
          swift --version
          ruby -v
          gem env home
          python3 --version || true
          node -v || true
          java -version || true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter doctor
        run: |
          set -euxo pipefail
          flutter --version
          flutter doctor -v

      - name: Flutter deps / clean
        run: |
          set -euxo pipefail
          flutter pub get
          flutter clean

      - name: Force iOS 15 in Podfile (idempotent)
        run: |
          set -euxo pipefail
          ruby -e "f='ios/Podfile'; c=File.read(f) rescue ''; if c != ''; c=c.gsub(/platform :ios, .*/, \"platform :ios, '15.0'\"); File.write(f,c); else; puts 'WARN: ios/Podfile not found yet'; end"

      - name: Ensure CocoaPods toolchain
        run: |
          set -euxo pipefail
          sudo gem install cocoapods --no-document
          pod --version
          pod repo list || true
          pod repo update

      - name: Summarize inputs (no secrets)
        run: |
          set -euxo pipefail
          echo "Bundle ID: ${BUNDLE_ID}"
          echo "Team ID present? $([[ -n "${TEAM_ID:-}" ]] && echo yes || echo NO)"
          echo "P12 present? $([[ -n "${P12_BASE64:-}" ]] && echo yes || echo NO)"
          echo "Mobileprovision present? $([[ -n "${MOBILEPROVISION_BASE64:-}" ]] && echo yes || echo NO)"
          echo "ASC_API_KEY envs defined? ID:$([[ -n "${{ secrets.ASC_API_KEY_ID }}" ]] && echo yes || echo NO) \
          Issuer:$([[ -n "${{ secrets.ASC_API_ISSUER_ID }}" ]] && echo yes || echo NO) \
          P8:$([[ -n "${{ secrets.ASC_API_KEY_P8_BASE64 }}" ]] && echo yes || echo NO)"

      - name: Stamp DEVELOPMENT_TEAM and manual signing in Xcode project
        run: |
          set -euxo pipefail
          PBX=ios/Runner.xcodeproj/project.pbxproj

          # Team
          sed -i '' "s/DEVELOPMENT_TEAM = [A-Z0-9]*/DEVELOPMENT_TEAM = ${TEAM_ID}/g" "$PBX" || true

          # Manual signing + modern identity
          sed -i '' "s/CODE_SIGN_STYLE = Automatic/CODE_SIGN_STYLE = Manual/g" "$PBX" || true
          sed -i '' "s/CODE_SIGN_IDENTITY = \"iPhone Developer\"/CODE_SIGN_IDENTITY = \"Apple Distribution\"/g" "$PBX" || true

          # Clear specifier in pbxproj (we'll drive via xcconfig)
          sed -i '' "s/PROVISIONING_PROFILE_SPECIFIER = .*/PROVISIONING_PROFILE_SPECIFIER = \"\"/g" "$PBX" || true

          # Also ensure DEV TEAM appears near bundle id, if missing
          perl -0777 -pe 's/(PRODUCT_BUNDLE_IDENTIFIER = '"$BUNDLE_ID"';)/$1\n\t\t\t\tDEVELOPMENT_TEAM = '"$TEAM_ID"';/g' \
            "$PBX" > /tmp/p.pbxproj || true
          if [ -s /tmp/p.pbxproj ]; then mv /tmp/p.pbxproj "$PBX"; fi

          echo "==== project.pbxproj signing excerpts ===="
          grep -nE "PRODUCT_BUNDLE_IDENTIFIER|DEVELOPMENT_TEAM|CODE_SIGN_STYLE|CODE_SIGN_IDENTITY|PROVISIONING_PROFILE" "$PBX" || true

      - name: Install fastlane
        run: |
          set -euxo pipefail
          gem install fastlane --no-document
          fastlane --version

      - name: Import signing certificate (keychain)
        env:
          P12_BASE64: ${{ secrets.P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
        run: |
          set -euxo pipefail
          echo "$P12_BASE64" | base64 --decode > signing.p12
          security create-keychain -p "" build.keychain
          security import signing.p12 -P "$P12_PASSWORD" -A -k build.keychain
          security list-keychains -s build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain
          echo "==== codesigning identities in build.keychain ===="
          security find-identity -v -p codesigning build.keychain || true

      # ---------- Parse provisioning profile ONCE ----------
      - name: Decode & parse provisioning profile
        shell: bash
        env:
          MOBILEPROVISION_BASE64: ${{ secrets.MOBILEPROVISION_BASE64 }}
        run: |
          set -euxo pipefail
          PP_DIR="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PP_DIR"

          if printf '%s' "$MOBILEPROVISION_BASE64" | base64 -D > "$PP_DIR/profile.mobileprovision" 2>/dev/null; then :
          elif printf '%s' "$MOBILEPROVISION_BASE64" | base64 --decode > "$PP_DIR/profile.mobileprovision" 2>/dev/null; then :
          elif printf '%s' "$MOBILEPROVISION_BASE64" | base64 -d > "$PP_DIR/profile.mobileprovision" 2>/dev/null; then :
          else
            echo "Failed to base64-decode MOBILEPROVISION_BASE64" >&2; exit 1
          fi

          ls -l "$PP_DIR"
          file "$PP_DIR/profile.mobileprovision" || true

          security cms -D -i "$PP_DIR/profile.mobileprovision" -o /tmp/profile.plist
          /usr/bin/plutil -lint /tmp/profile.plist

          NAME=$(/usr/libexec/PlistBuddy -c 'Print :Name' /tmp/profile.plist)
          UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' /tmp/profile.plist)
          APPID=$(/usr/libexec/PlistBuddy -c 'Print :Entitlements:application-identifier' /tmp/profile.plist || true)

          : "${NAME:?Provisioning profile Name missing}"
          : "${UUID:?Provisioning profile UUID missing}"

          echo "PROFILE_NAME=$NAME" >> $GITHUB_ENV
          echo "PROFILE_UUID=$UUID" >> $GITHUB_ENV

          echo "Resolved profile: $NAME ($UUID)"
          echo "App Identifier in profile: ${APPID:-unknown}"

          # Save plist for artifact
          mkdir -p build-logs
          cp /tmp/profile.plist build-logs/profile.plist

      # ---------- Inject signing into Release.xcconfig ONCE ----------
      - name: Inject signing into Release.xcconfig (safe)
        shell: bash
        env:
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          PROFILE_NAME: ${{ env.PROFILE_NAME }}
        run: |
          set -euxo pipefail
          : "${PROFILE_NAME:?PROFILE_NAME not found. Ensure the decode/parse step ran successfully.}"

          XCCONFIG="ios/Flutter/Release.xcconfig"

          # Ensure CocoaPods include exists (no rewriting)
          grep -q 'Pods-Runner.release.xcconfig' "$XCCONFIG" || \
            echo '#include "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"' >> "$XCCONFIG"

          # Remove previously injected block (idempotent)
          sed -i '' '/^\/\/ --- Signing injected by CI ---/,$d' "$XCCONFIG" || true

          {
            echo '// --- Signing injected by CI ---'
            echo "PRODUCT_BUNDLE_IDENTIFIER = ${BUNDLE_ID}"
            echo 'CODE_SIGN_STYLE = Manual'
            echo 'CODE_SIGN_IDENTITY = Apple Distribution'
            echo "PROVISIONING_PROFILE_SPECIFIER = ${PROFILE_NAME}"
            echo "DEVELOPMENT_TEAM = ${TEAM_ID}"
          } >> "$XCCONFIG"

          echo "==== Final Release.xcconfig ===="
          cat "$XCCONFIG"

          mkdir -p build-logs
          cp "$XCCONFIG" build-logs/Release.xcconfig

      - name: Pod install (log & artifact)
        working-directory: ios
        run: |
          set -euxo pipefail
          pod install | tee ../build-logs/pod_install.log

      - name: Show Xcode build settings (after pods)
        working-directory: ios
        run: |
          set -euxo pipefail
          xcodebuild -workspace Runner.xcworkspace -scheme Runner -configuration Release -showBuildSettings > ../build-logs/xcodebuild_showBuildSettings.txt

      - name: Build IPA (Release, verbose)
        run: |
          set -euxo pipefail
          # Flutter verbose already prints most of what we need
          flutter build ipa --release --verbose
          ls -l build/ios/ipa || true
          # Keep a copy of the xcarchive if present
          if ls build/ios/archive/*.xcarchive 1>/dev/null 2>&1; then
            echo "Found xcarchive(s):"
            ls -l build/ios/archive
          fi

      - name: Prepare App Store Connect API key
        working-directory: ios
        env:
          ASC_API_KEY_ID: ${{ secrets.ASC_API_KEY_ID }}
          ASC_API_ISSUER_ID: ${{ secrets.ASC_API_ISSUER_ID }}
          ASC_API_KEY_P8_BASE64: ${{ secrets.ASC_API_KEY_P8_BASE64 }}
        run: |
          set -euxo pipefail
          mkdir -p fastlane
          echo "$ASC_API_KEY_P8_BASE64" | base64 --decode > fastlane/AuthKey_${ASC_API_KEY_ID}.p8
          cat > fastlane/api_key.json <<JSON
          {
            "key_id": "${ASC_API_KEY_ID}",
            "issuer_id": "${ASC_API_ISSUER_ID}",
            "key": "$(awk '{printf "%s\\n", $0}' fastlane/AuthKey_${ASC_API_KEY_ID}.p8)"
          }
          JSON
          echo "Wrote fastlane/api_key.json (redacted)"

      - name: Upload to TestFlight (fastlane pilot, verbose)
        working-directory: ios
        run: |
          set -euxo pipefail
          IPA="../build/ios/ipa/Runner.ipa"
          if [ ! -f "$IPA" ]; then
            echo "ERROR: IPA not found at $IPA"
            exit 1
          fi
          fastlane pilot upload \
            --api_key_path fastlane/api_key.json \
            --ipa "$IPA" \
            --verbose \
            --skip_waiting_for_build_processing false

      - name: Save IPA artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: build/ios/ipa/*.ipa
          if-no-files-found: warn

      - name: Upload troubleshooting bundle (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-troubleshooting-bundle
          path: |
            build-logs/**
            ios/Runner.xcodeproj/project.pbxproj
            ios/Runner.xcworkspace/contents.xcworkspacedata
            ios/Podfile
            ios/Podfile.lock
            ios/Pods/Manifest.lock
          if-no-files-found: warn
