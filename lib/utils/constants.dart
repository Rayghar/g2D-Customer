// File: lib/utils/constants.dart
import 'package:flutter/material.dart';

// For local development with backend on the same machine:
const String baseUrl =
    'https://primejet-backend.onrender.com/api/v1'; //'http://10.0.2.2:3000/api/v1';
//'https://primejet-backend.onrender.com/api/v1'; // Assuming backend on port 3000

// If testing on iOS Simulator or Web (backend on same machine): const String baseUrl = 'http://localhost:3000';
// If testing on Physical Device (replace YOUR_LAPTOP_IP): const String baseUrl = 'http://YOUR_LAPTOP_IP:3000';

const Color primaryBlue = Color(0xFF324681);
const Color primaryRed = Color(0xFFd7000f);
const Color backgroundGray = Color(0xFFf0f0f0);
const Color cardWhite = Color(0xFFffffff);
